using LinearAlgebra
using Random
using Distributed

function rADMM_LS(A,b,maxiter)
    lambda = 0
    x = 0
    return x,lambda
end

function rADMM_Ridge(A,b,maxiter)
    lambda = 0
    x = 0
    return x,lambda
end

function rADMM_QuadReg(A,b,maxiter)
    lambda = 0
    x = 0
    return x,lambda
end

function rADMM_SOCP(A,b,maxiter)
    lambda = 0
    x = 0
    return x,lambda
end

function preprocessA(A)
    #fast walsh goes here

    return x,lambda
end

function preprocessb(b)

    return x,lambda
end
